# 🧪 测试步骤

## ✅ 修复完成！

CORS 问题已修复，现在可以从 `file://` 协议访问服务器了。

---

## 📝 测试步骤

### 1️⃣ 单人测试

1. 用浏览器打开：
   ```
   file:///F:/KimiProjects/poker_arena_new/poker_arena_server/test-client.html
   ```

2. 点击 **"🔌 Connect"** 按钮

3. 应该看到：
   ```
   🟢 Connected: tN3eB5jvm921RrjsAAAB (你的 socket ID)
   ```

4. 输入玩家名字（或使用默认的 "Test Player"）

5. 点击 **"📝 Create Room"**

6. 应该看到：
   ```
   🎮 Room created successfully!
   Room ID: xxxxxxxx (8位房间号)
   ```

---

### 2️⃣ 多人测试

1. **玩家 1** 创建房间（按上面步骤）

2. **玩家 2** 在新的浏览器标签页打开相同页面

3. 玩家 2 点击 **"Connect"**

4. 玩家 2 输入玩家 1 的房间号

5. 玩家 2 点击 **"🚪 Join Room"**

6. 两边应该都看到：
   ```
   👥 Player joined: [玩家名]
   ```

7. 两个玩家都点击 **"✋ Ready"**

8. 当所有人都准备好后，应该看到：
   ```
   🎲 Game started with 2 players!
   ```

---

### 3️⃣ 查看服务器统计

点击 **"📊 Get Server Stats"** 按钮，应该看到：
```
📊 Server Stats:
   Rooms: 1
   Players: 2
   Room Details:
   - xxxxxxxx: 2 players, state: playing
```

---

## 🐛 如果还是无法连接

### 检查 1: 服务器是否运行
```bash
curl http://localhost:3000/health
```
应该返回：`{"status":"ok","timestamp":"..."}`

### 检查 2: 查看浏览器控制台
按 `F12` 打开开发者工具，查看 Console 是否有错误

### 检查 3: 查看服务器日志
服务器应该输出：
```
[GameServer] Client connected: xxxxxx
```

### 检查 4: 防火墙
确保防火墙没有阻止 3000 端口

---

## 📊 预期的连接流程

```
浏览器                                服务器
  |                                     |
  |-------- WebSocket 连接 ----------->|
  |                                     |
  |<------- connect 事件 --------------|
  |                                     | [GameServer] Client connected: xxx
  |                                     |
  |-------- create_room --------------->|
  |                                     | [GameServer] Room created: xxx
  |<------- room_created --------------|
  |                                     |
```

---

## 🎯 现在尝试测试吧！

刷新浏览器页面，点击 Connect，应该可以正常工作了！ 🚀
